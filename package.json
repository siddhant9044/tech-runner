{
  "name": "tech-runner-multidomain",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "check": "npm run check --prefix backend && npm run build --prefix frontend"
  }
}
